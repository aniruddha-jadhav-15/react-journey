# Module 8 — React Design Patterns

## Container / Presentational Pattern

Split components into two parts:

- Container — handles logic and data fetching
- Presentational — handles UI only

Container passes data to Presentational via props.
Keeps logic and UI separate and reusable.

💡 Key Takeaway
Single responsibility — one component does one job.
